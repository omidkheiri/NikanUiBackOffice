import React, { Fragment } from "react";
import { useParams } from "react-router-dom";
const SupplierDetail = () => {
  const params = useParams();
  return (
    <Fragment>
      <h1>Supplaier Detail</h1>
    </Fragment>
  );
};

export default SupplierDetail;
